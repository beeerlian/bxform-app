type Mutation {
  register(
    body: RegisterInput!
  ): RegisterOutput
}

input RegisterInput {
  name: String!
  email: String!
  password: String!
}

type RegisterOutput {
  accessToken: String!
  name: String!
  email: String!
}

